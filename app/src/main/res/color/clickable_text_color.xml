<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Hover Effect (Brighter version of primary) -->
    <item android:color="@color/primary_variant" android:state_hovered="true"/>
    <!-- Default State -->
    <item android:color="@color/primary"/>
</selector>
